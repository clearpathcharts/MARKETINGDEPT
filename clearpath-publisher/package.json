{
  "name": "clearpath-publisher",
  "version": "1.0.0",
  "description": "ClearPath Publisher backend — persistent queue, scheduler, and direct platform dispatch. No Zapier, no Buffer.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
